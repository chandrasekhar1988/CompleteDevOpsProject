apiVersion: apps/v1
kind: Deployment
metadata: { name: ecom-deployment }
spec:
  replicas: 3 # ప్రొడక్షన్ లో ఎల్లప్పుడూ 3 కాపీలు రన్ అవుతుంటాయి
  template:
    spec: { containers: [ { name: ecom-app, image: ecom-app:v1 } ] }
